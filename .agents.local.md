# .agents.local.md — Local Agent Scratchpad

> Gitignored. Never committed. Persistent memory across agent sessions.

## Preferences

- **Style:** Direct, actionable, educational tone. This is about helping teams adopt AI practices—be practical.
- **Code:** Minimal changes. Preserve existing vanilla JS patterns. Enhance accessibility; don't rebuild.
- **Planning:** Always state the plan before writing code. Validate changes in browser (local server) before committing.

## Patterns

<!-- Settled truths about this project. Promote recurring session learnings here. -->

## Gotchas

<!-- Things that look right but aren't. Include WHY. -->

## Dead Ends

<!-- Approaches tried and failed. Include WHY they failed. -->

## Ready to Promote

<!-- The agent flags items here during compression when a pattern has recurred across 3+ sessions.
     The human decides when to move them into AGENTS.md's Project Knowledge section.
     Use the same pipe-delimited format AGENTS.md expects:
       pattern | where-to-see-it    → goes in Patterns
       rule | reason                → goes in Boundaries
       trap | fix                   → goes in Gotchas
-->

## Session Log

<!-- Append new entries at the END. One entry per session. Keep each to 5-10 lines. -->

### YYYY-MM-DD — Topic

- **Done:** (what changed)
- **Worked:** (reuse this)
- **Didn't work:** (avoid this)
- **Decided:** (choices and reasoning)
- **Learned:** (new patterns or gotchas)

---

## Compression Log

<!-- When this file exceeds 300 lines, compress. Log it here. -->

### 2026-09-01 — Framework refresh for the Copilot app era

- **Done:** Updated README, all five docs pages, and seven templates for mid-2026 GitHub Copilot: Copilot app (GA Jun 17, all plans Jul 7), cloud agent rename (Apr 1), CLI GA + /fleet + Rubber Duck + /security-review, Agent Plugins 1.0, Agent Skills, enterprise managed settings + MCP allowlists, Sept 1 model retirements, metrics impact dashboard.
- **Worked:** Python string-replace scripts with `assert count == 1` guards for HTML edits; Playwright smoke test against `python -m http.server --directory docs` with pageerror/console listeners; keep localStorage keys (`copilotSpaces`) and only change titles/descriptions.
- **Didn't work:** github.blog, docs.github.com, and code.visualstudio.com are blocked by the egress proxy — use WebSearch summaries plus github.com raw files (copilot-cli changelog.md, github/app README) for primary detail.
- **Decided:** Kept the 4-question decision tree logic untouched; added a "Verification" matrix row instead of a fifth question. Added agent dimensions to `templates/team-maturity-assessment.md` to match the HTML tool.
- **Learned:** Pre-existing bug: `loadFromLocalStorage()` in team-assessment.html restores `.selected` with `[onclick*="${category}, ${level}"]`, which never matches `selectRating('key', n, event)` (quote between key and comma). Scores restore, highlights don't. Not fixed (out of scope).
